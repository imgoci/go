[project]
name = "imgoci-go-docs"
version = "0.0.0"
description = "MkDocs documentation site for imgoci/go."
requires-python = ">=3.14"
dependencies = [
    "mkdocs-material>=9.7.7",
]

[tool.uv]
package = false
